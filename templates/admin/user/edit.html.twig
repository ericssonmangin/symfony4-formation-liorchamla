{% extends 'admin/base.html.twig' %}

{% block title %} Admin - Edition de l'utilisateur {% endblock %}

{% block body %}

    <div class="container">
        <h1 class="my-5">Modification de l'utilisateur : {{ user.fullName }}</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Enregistrer les modifications &nbsp; <i class="fas fa-share"></i></button>
        {{ form_end(form) }}
    </div>

{% endblock %}